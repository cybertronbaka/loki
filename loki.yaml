name: dev
description: This is for development purposes
packages:
  - ../loki_test/packages
scripts:
  pub:
    name: pub
    description: Description
    exec: dart pub get
    working_dir: ../loki_test/
  activate:
    name: activate loki
    exec: dart pub global activate --source path . --overwrite
    working_dir: .
  test1:
    name: Test1
    description: Running Test 1
    exec: loki run activate && loki run pub && cd ../loki_test/packages/test1 && flutter run -d linux
    stdin: true